% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{chorale_fit}
\alias{chorale_fit}
\title{Match phenotype-responsive factors across modalities}
\usage{
chorale_fit(
  containers,
  n_factors,
  gene_sets = NULL,
  feature_map = NULL,
  feature_space = NULL,
  transform = "auto",
  strata_keys = NULL,
  profile_covariates = NULL,
  bound_strata = NULL,
  assay_name = NULL,
  control = chorale_control(),
  seed = 1L,
  ...
)
}
\arguments{
\item{containers}{A named list of \link[SummarizedExperiment:SummarizedExperiment]{SummarizedExperiment::SummarizedExperiment}
objects, one per modality, as returned by \code{\link[=chorale_load]{chorale_load()}}.}

\item{n_factors}{Integer, or one integer per modality, giving the number of
components to recover, from the detectability gate. \code{"auto"} sets it per
modality by parallel analysis, through \code{\link[=chorale_n_factors]{chorale_n_factors()}}.}

\item{gene_sets}{A named list of curated gene sets, as returned by
\code{\link[=chorale_genesets]{chorale_genesets()}}. Without it, markers are selected on loadings alone
and factors carry no pathway definition.}

\item{feature_map}{Optional named list, one entry per modality, each a data
frame from \code{\link[=chorale_map]{chorale_map()}} harmonising that modality's features to Entrez
identifiers.}

\item{feature_space}{Optional named character vector, one entry per
modality, saying what its features are: \code{"gene"} for a transcriptome or
proteome, whose identifiers reach the curated sets through
\code{\link[=chorale_map]{chorale_map()}}, or \code{"lipid"} for a lipidome, whose features reach them
through their class by \code{\link[=chorale_metabolite_matrix]{chorale_metabolite_matrix()}}. Defaults to \code{"gene"}
everywhere. It is declared rather than guessed, since a wrong guess would
silently leave a modality out of the pathway comparison.}

\item{transform}{Per-modality measurement-model transform, one of \code{"auto"},
\code{"none"}, \code{"log"} or \code{"vst"}, given as a single value or a named vector.
See \code{\link[=chorale_transform]{chorale_transform()}}. Defaults to \code{"auto"}, which reads the transform
from each matrix.}

\item{strata_keys}{Design columns defining an anchoring stratum. \code{NULL}, the
default, uses every covariate the designs share. Deprecated: use
\code{profile_covariates} for matching and \code{bound_strata} for bounds.}

\item{profile_covariates}{Optional covariates allowed to refine the mandatory
phenotype-led match. \code{NULL} discovers every eligible shared covariate.}

\item{bound_strata}{Covariates defining strata for coupling bounds. \code{NULL}
uses the eligible shared covariates.}

\item{assay_name}{Assay to take from each container.}

\item{control}{Every decision the run takes, as \code{\link[=chorale_control]{chorale_control()}}: the
threshold a programme must beat, the permutation counts, what counts as a
pure feature, and the rest. It travels with the fit, so a result records
what decided it.}

\item{seed}{Integer seed.}

\item{...}{Named settings overriding \code{control}, so changing one decision does
not mean restating the others.}
}
\value{
An object of class \code{chorale_fit}.
}
\description{
Components are recovered per modality by independent component analysis over
several initialisations. Pure features are then selected on the loadings
alone, so the identification argument holds before any prior is applied, with
ties broken towards curated-set coherence. The remaining loadings are
expressed in the curated vocabulary and stored beside the fit as a
description of it, with the variance each explains reported.
}
\details{
Factors are then matched across modalities on their design-effect profiles,
and the assignment is solved once over the whole collection rather than a
pair at a time, so the correspondences agree around every cycle. Each
resulting programme is scored as one object against a null that reruns the
complete procedure on permuted designs. Where curated sets are supplied the
programmes are corroborated a second time on biology, against an
score-residual null, and the two channels are reported separately.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 60,
                        n_shared_factors = 2, n_private_factors = 1,
                        n_strains = 2, n_per_cell = 2, seed = 1)
containers <- Map(chorale_load, sim$modalities, sim$col_data)
fit <- chorale_fit(containers, n_factors = c(3, 3), n_init = 2)
fit
}
