% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concept_fit.R
\name{chorale_concept_fit}
\alias{chorale_concept_fit}
\title{Estimate the shared state of a collection through its concepts}
\usage{
chorale_concept_fit(
  containers,
  sets,
  feature_space = NULL,
  feature_map = NULL,
  n_free = "auto",
  transform = "auto",
  n_permutations = 999L,
  min_features = 5L,
  min_modalities = 1L,
  assay_name = NULL,
  control = chorale_control(),
  seed = 1L,
  ...
)
}
\arguments{
\item{containers}{A named list of \code{\link[=chorale_load]{chorale_load()}} containers.}

\item{sets}{A named list of concepts, as returned by \code{\link[=chorale_genesets]{chorale_genesets()}}, or
a \code{chorale_concepts} object already built for this collection.}

\item{feature_space, feature_map}{Passed to \code{\link[=chorale_concepts]{chorale_concepts()}}.}

\item{n_free, transform}{Passed to \code{\link[=chorale_encode]{chorale_encode()}}.}

\item{n_permutations}{Permutations calibrating the vocabulary.}

\item{min_features}{Features of a modality a concept must reach before that
modality is counted as expressing it.}

\item{min_modalities}{Modalities a concept must reach to enter the
vocabulary.}

\item{assay_name}{Assay to read from each container.}

\item{control}{A \code{\link[=chorale_control]{chorale_control()}} object.}

\item{seed}{Integer seed.}

\item{...}{Named overrides applied to \code{control}.}
}
\value{
An object of class \code{chorale_concept_fit} carrying \code{concepts},
\code{encoding} and \code{evidence}.
}
\description{
The whole estimator in one call. A vocabulary of named concepts is built for
the collection, every modality is scored on it, what the vocabulary does not
explain is kept as free dimensions, and the design is asked whether each
concept changes with the configured phenotype contrast.
}
\details{
The order matters and is fixed. Scoring comes before the design is consulted,
so the concept scores cannot have been shaped by the contrast they are then
tested on. The vocabulary is fixed before any of it, so which concepts are
tested does not depend on which of them turn out to be interesting.

What connects the modalities is the vocabulary, not the design. Two
modalities measured on different individuals share no sample and need share no
covariate beyond the phenotype: they meet because both are scored on the same
named concepts. The design is what supplies the evidence, one modality at a
time, that a concept moves with the phenotype.
}
\examples{
fx <- chorale_concept_example(seed = 1)
chorale_concept_fit(fx$containers, fx$sets, n_free = 1,
                    n_permutations = 99, n_init = 2)
}
