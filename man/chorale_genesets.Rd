% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genesets.R
\name{chorale_genesets}
\alias{chorale_genesets}
\title{Retrieve the concepts a collection is scored on}
\usage{
chorale_genesets(
  collections = c("hallmark", "reactome", "cell_type"),
  species = "Mus musculus",
  db_species = c("MM", "HS"),
  min_size = 10L,
  max_size = 500L,
  registry = chorale_geneset_registry(),
  custom = NULL
)
}
\arguments{
\item{collections}{Character vector naming entries of \code{registry}.}

\item{species}{Species the identifiers are returned for, as understood by
\code{\link[msigdbr:msigdbr]{msigdbr::msigdbr()}}, for example \code{"Mus musculus"} or \code{"Homo sapiens"}.}

\item{db_species}{MSigDB source database, \code{"MM"} for the mouse-native
collections or \code{"HS"} for the human ones. Switching an analysis from
mouse to human is this argument and \code{species} together.}

\item{min_size, max_size}{Retain sets whose membership falls within this
range, counted after mapping to Entrez identifiers.}

\item{registry}{A registry as returned by \code{\link[=chorale_geneset_registry]{chorale_geneset_registry()}}.}

\item{custom}{An optional named list of character vectors of Entrez
identifiers, appended to the retrieved collections. Supplying \code{custom}
alone, with \code{collections = character()}, replaces MSigDB entirely.}
}
\value{
A named list of character vectors of Entrez gene identifiers, with
an attribute \code{collection} recording which collection each set came from.
}
\description{
Returns curated sets as Entrez identifiers, the space \code{\link[=chorale_map]{chorale_map()}}
harmonises features into. Those sets are the vocabulary of named concepts
that connects modalities measured on different individuals: every modality is
scored on the same concepts, and a concept is the same concept in every
modality because its name and its membership are fixed before any data are
looked at.
}
\details{
Sets far outside the size window are dropped. Very small sets are matched by
chance and very large ones name nothing specific, so both weaken a concept
rather than sharpening it.
}
\examples{
\dontshow{if (rlang::is_installed("msigdbr")) withAutoprint(\{ # examplesIf}
sets <- chorale_genesets("hallmark", species = "Mus musculus")
length(sets)
\dontshow{\}) # examplesIf}
}
