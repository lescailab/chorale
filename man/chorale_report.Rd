% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concept_report.R, R/report.R
\name{chorale_report.chorale_concept_fit}
\alias{chorale_report.chorale_concept_fit}
\alias{chorale_report}
\alias{chorale_report.chorale_fit}
\title{Emit interpretable outputs from a fit}
\usage{
\method{chorale_report}{chorale_concept_fit}(fit, null = NULL, path, n_top = 10L, ...)

chorale_report(fit, ...)

\method{chorale_report}{chorale_fit}(fit, bound = NULL, null = NULL, path, n_top_sets = 5L, ...)
}
\arguments{
\item{fit}{A fit to write out: a \code{chorale_concept_fit} or a \code{chorale_fit}.}

\item{null}{A \code{chorale_null} object, as returned by \code{\link[=chorale_null]{chorale_null()}}.}

\item{path}{Directory to write into. Created if absent.}

\item{n_top}{Features named per free dimension.}

\item{...}{Passed to the method.}

\item{bound}{A \code{chorale_bound} object, as returned by \code{\link[=chorale_bound]{chorale_bound()}}.
Applies to a \code{chorale_fit} only.}

\item{n_top_sets}{Number of curated sets naming each factor.}
}
\value{
Invisibly, a character vector of the files written.
}
\description{
Writes the complete fit, controls and interpretation tables. Every factor
carries a pathway definition and its marker features; a factor that cannot
be named is written to the same tables marked unresolved, rather than
dropped, since an unnamed factor is a finding about
the gene sets rather than an absence.
}
\details{
Cross-modality disagreement is reported alongside agreement. Factors moving
in opposite directions are aligned for comparison and their orientation is
retained rather than silently discarded.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 120,
                        n_shared_factors = 2, n_private_factors = 1,
                        n_strains = 4, n_per_cell = 3, effect_size = 3,
                        seed = 1)
containers <- Map(chorale_load, sim$modalities, sim$col_data)
fit <- chorale_fit(containers, n_factors = c(3, 3), n_init = 2,
                   n_ambiguity_boot = 19)
out <- chorale_report(fit, chorale_bound(fit), NULL,
                      path = withr::local_tempdir())
basename(out)
}
