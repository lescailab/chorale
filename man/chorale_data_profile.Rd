% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{chorale_data_profile}
\alias{chorale_data_profile}
\title{Summarise the properties of a real matrix a simulation must reproduce}
\usage{
chorale_data_profile(
  assay,
  col_data = NULL,
  covariates = NULL,
  n_quantiles = 25L,
  n_eigen = 50L,
  layer = NULL
)
}
\arguments{
\item{assay}{A feature-by-sample numeric matrix, on the scale the estimator
would read it. Missing values are recorded rather than imputed.}

\item{col_data}{Optional design table for the same samples, one row per
column of \code{assay}, from which the design margins are taken.}

\item{covariates}{Design columns to profile. \code{NULL}, the default, takes every
column except \code{sample_id}. Name the columns explicitly to keep identifying
fields out of the profile.}

\item{n_quantiles}{Number of points in the per-feature quantile grid. The
grid is what a simulated feature is mapped onto, so it sets how finely a
marginal is reproduced.}

\item{n_eigen}{Number of leading eigenvalues retained from the correlation
structure.}

\item{layer}{Optional name recorded with the profile.}
}
\value{
An object of class \code{chorale_data_profile}.
}
\description{
The estimator reads a normalised feature-by-sample matrix, so the properties
a simulation has to get right are the properties of that matrix rather than
of the measurement that produced it. Six of them are what the estimator can
respond to: the per-feature marginal distribution, the mean-variance
relation, missingness as a function of abundance, the feature-feature
correlation structure, the non-Gaussianity identification rests on, and the
margins of the design. This function measures all six and returns them as one
object, which \code{\link[=chorale_simulate]{chorale_simulate()}} consumes to give simulated data the shape
of real data without reproducing the measurement model that produced it.
}
\details{
The object holds summaries, not data. No sample identifier enters it and no
value of any covariate the caller withholds. Feature identifiers are kept,
because a planted pathway is defined on them.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 40, seed = 1)
chorale_data_profile(sim$modalities[[1]], sim$col_data[[1]],
                     covariates = c("phenotype", "sex"))
}
