% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genesets.R
\name{chorale_geneset_matrix}
\alias{chorale_geneset_matrix}
\title{Represent gene sets as a feature-by-set indicator matrix}
\usage{
chorale_geneset_matrix(feature_ids, sets, weights = NULL, min_features = 5L)
}
\arguments{
\item{feature_ids}{Character vector of Entrez identifiers, one per feature,
in the row order of the modality's assay.}

\item{sets}{A named list of gene sets, as returned by \code{\link[=chorale_genesets]{chorale_genesets()}}.}

\item{weights}{Optional numeric vector, one per feature, giving the
fractional weight from \code{\link[=chorale_map]{chorale_map()}}. Defaults to 1 for every feature.}

\item{min_features}{Drop sets matching fewer than this many features of the
modality, since a set that barely intersects the measured features cannot
define a factor in it.}
}
\value{
A numeric matrix with one row per feature and one column per
retained gene set.
}
\description{
Builds the feature-by-set matrix the pathway layer reads: rows are
the features of one modality, columns are gene sets, and an entry is 1 where
the feature belongs to the set. Features carry fractional weight where an
identifier maps to several genes, so a one-to-many mapping contributes
proportionally rather than counting once per gene.
}
\examples{
sets <- list(set_a = c("1", "2", "3"), set_b = c("3", "4"))
chorale_geneset_matrix(c("1", "2", "3", "4"), sets, min_features = 2)
}
