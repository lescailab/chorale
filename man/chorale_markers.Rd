% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{chorale_markers}
\alias{chorale_markers}
\title{Select pure features anchoring each factor}
\usage{
chorale_markers(
  loadings,
  purity_ratio = 0.25,
  min_markers = 2L,
  max_markers = 20L,
  prior = NULL
)
}
\arguments{
\item{loadings}{A features-by-factors numeric matrix.}

\item{purity_ratio}{A feature is pure for a factor when the largest
competing absolute loading is at most this fraction of its own.}

\item{min_markers}{Minimum qualifying features per factor.}

\item{max_markers}{Maximum markers retained per factor.}

\item{prior}{Optional feature-by-set indicator matrix from
\code{\link[=chorale_geneset_matrix]{chorale_geneset_matrix()}}, used only to break ties.}
}
\value{
A list with \code{markers}, the features per factor that clear the
purity threshold; \code{best_candidates}, the most nearly pure features whether
or not they clear it; \code{purity_margin}, the competing-to-own loading ratio
of the best candidates; and \code{pure_feature_condition}, recording which
factors reached \code{min_markers}.
}
\description{
A pure feature loads strongly on one factor and weakly on the others. This is
a loading-concentration diagnostic; CHORALE does not use it as proof that a
latent factor is uniquely recovered.
}
\details{
Purity is calculated first, on the loadings alone. Where more features
qualify than are retained, the tie is broken on biology: candidates sharing a
curated set with the other candidates for the same factor are preferred. Any
qualifying set satisfies the condition equally, so choosing the coherent one
costs nothing and yields markers that can be read.
}
\examples{
set.seed(1)
l <- matrix(rnorm(60), nrow = 20, dimnames = list(paste0("f", 1:20), NULL))
l[1:2, 2:3] <- 0
chorale_markers(l)$markers[[1]]
}
