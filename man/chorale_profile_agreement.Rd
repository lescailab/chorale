% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{chorale_profile_agreement}
\alias{chorale_profile_agreement}
\title{Compare two matrices on the properties a simulation must reproduce}
\usage{
chorale_profile_agreement(simulated, reference)
}
\arguments{
\item{simulated}{A \code{chorale_data_profile} of the simulated matrix.}

\item{reference}{A \code{chorale_data_profile} of the matrix the simulation was
built to resemble.}
}
\value{
A data frame with one row per property, carrying its value in each
profile and the discrepancy between them.
}
\description{
Places a simulated matrix beside the real one its profile was taken from,
on the six properties \code{\link[=chorale_data_profile]{chorale_data_profile()}} measures. The comparison is
what a claim of realism is bounded to: agreement here says the simulated
matrix is indistinguishable from the real one on what the estimator reads,
and says nothing about the measurement process.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 40, seed = 1)
a <- chorale_data_profile(sim$modalities[[1]])
b <- chorale_data_profile(sim$modalities[[2]])
chorale_profile_agreement(a, b)
}
