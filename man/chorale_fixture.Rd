% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{chorale_fixture}
\alias{chorale_fixture}
\title{Read a committed test fixture}
\usage{
chorale_fixture(layer = c("RNA", "PROT", "METAB"), path = NULL)
}
\arguments{
\item{layer}{Character scalar, one of \code{"RNA"}, \code{"PROT"} or \code{"METAB"}.}

\item{path}{Directory holding the fixtures. Defaults to the installed
fixture directory, and resolves under \code{testthat::test_path()} when called
from a test.}
}
\value{
A list with \code{assay}, a feature-by-sample numeric matrix, and
\code{col_data}, the matching per-sample design table.
}
\description{
Reads one of the synthetic fixture matrices shipped with the package,
returning a feature-by-sample numeric matrix with its identifiers
restored. The fixtures are generated deterministically by
\code{data-raw/fixtures.R}; they contain generic identifiers and controlled
missingness at a size continuous integration can run.
}
\examples{
\dontshow{if (dir.exists(system.file("fixtures", package = "chorale"))) withAutoprint(\{ # examplesIf}
fx <- chorale_fixture("RNA")
dim(fx$assay)
\dontshow{\}) # examplesIf}
}
