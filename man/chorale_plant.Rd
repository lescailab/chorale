% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plant.R
\name{chorale_plant}
\alias{chorale_plant}
\title{Plant named concepts in a simulated collection}
\usage{
chorale_plant(
  profiles,
  membership,
  plant_sets,
  score_sets,
  n_concepts = 3L,
  n_private_factors = 2L,
  min_modalities = 2L,
  min_features = 10L,
  member_fraction = 0.6,
  leak_fraction = 0.2,
  n_markers = 5L,
  background_sd = 0.2,
  seed = 1L,
  ...
)
}
\arguments{
\item{profiles}{A named list of \code{\link[=chorale_data_profile]{chorale_data_profile()}} objects, one per
modality, carrying the real feature identifiers a pathway is defined on.}

\item{membership}{A named list matching \code{profiles}, each a feature-by-set
matrix placing that modality's features in the planting vocabulary, from
\code{\link[=chorale_geneset_matrix]{chorale_geneset_matrix()}} for genes or \code{\link[=chorale_metabolite_matrix]{chorale_metabolite_matrix()}} for
lipids. Row names must be the profile's feature identifiers.}

\item{plant_sets}{The planting collection, a named list of member
identifiers, as returned by \code{\link[=chorale_genesets]{chorale_genesets()}}.}

\item{score_sets}{The collection recovery is scored in, which is the planting
collection itself. The realised overlap between planted sets travels with
the result.}

\item{n_concepts}{Number of concepts to plant.}

\item{n_private_factors}{Modality-private factors carrying no pathway and no
cross-modality partner.}

\item{min_modalities}{Modalities a set must reach to be plantable.}

\item{min_features}{Features a set must match in a modality to reach it.}

\item{member_fraction}{Share of a set's features in a modality that carry the
concept.}

\item{leak_fraction}{Share of the features receiving the concept's signal
that lie outside the set. A count, not a share of loading mass: carrier
and leak amplitudes are drawn from the same distribution, so the two
coincide in expectation but only the count is enforced.}

\item{n_markers}{Members per concept made pure, so the pure features of a
recovered dimension can be checked against the concept.}

\item{background_sd}{Standard deviation of the loadings of features carrying
no concept.}

\item{seed}{Integer seed.}

\item{...}{Passed to \code{\link[=chorale_simulate]{chorale_simulate()}}, for example \code{effect_size} or
\code{noise_sd}.}
}
\value{
A list with \code{sim}, the \code{\link[=chorale_simulate]{chorale_simulate()}} output; \code{plant}, one row
per concept and modality recording the set, its size, the features that
received loading and the markers among them; \code{sets}, the planted sets in
their planting vocabulary; \code{concepts}, their names; and \code{planted}, the label
each was given in the simulation against the set it was planted from.
}
\description{
Simulated data in which the shared structure is named concepts rather than
anonymous factors. Each planted concept occupies the features that belong to
it in every modality that can express it: the genes of the set in a
transcriptome or proteome, and the lipid classes the same set acts on in a
lipidome. Recovery can then be scored on biology, which no real cohort
supports, since no real cohort records which features take part in a
concept.
}
\details{
A concept is planted and recovered by name, so the planting and scoring
vocabularies are the same one. The question is not whether a concept\'s
composition can be rediscovered but whether the design shows it separating
phenotype groups in every modality that expresses it, and whether the
concepts that were not planted stay quiet.

Two dials govern how far a planted signal departs from a clean set
indicator. \code{member_fraction} is the share of a set's features that carry the
concept, and \code{leak_fraction} is the share of the features receiving that
concept's signal that lie outside the set. A planted truth is therefore
recorded as the features that actually received loading, not as the nominal
pathway, and identifier mapping losses are absorbed the same way.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plant <- chorale_plant(profiles, membership, kegg, reactome)
\dontshow{\}) # examplesIf}
}
