% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode.R
\name{chorale_encode}
\alias{chorale_encode}
\title{Score every modality on the shared concepts, and keep what they miss}
\usage{
chorale_encode(
  containers,
  concepts,
  n_free = "auto",
  transform = "auto",
  assay_name = NULL,
  control = chorale_control(),
  seed = 1L,
  ...
)
}
\arguments{
\item{containers}{A named list of \code{\link[=chorale_load]{chorale_load()}} containers.}

\item{concepts}{A \code{chorale_concepts} object for the same collection.}

\item{n_free}{Free dimensions per modality: one number, one per modality, a
named vector, or \code{"auto"} to choose the count by reproducibility, with
\code{\link[=chorale_select_factors]{chorale_select_factors()}} holding it to what parallel analysis on the
residual says the data could support.}

\item{transform}{Per-modality scale handling, as in \code{\link[=chorale_transform]{chorale_transform()}}.}

\item{assay_name}{Assay to read from each container. Defaults to the first.}

\item{control}{A \code{\link[=chorale_control]{chorale_control()}} object.}

\item{seed}{Integer seed.}

\item{...}{Named overrides applied to \code{control}.}
}
\value{
An object of class \code{chorale_encode} with \code{encodings}, one entry per
modality carrying \code{concept_scores}, \code{free_scores} and \code{free_loadings};
\code{variance}, one row per modality giving the share of variance the concepts
carry, the share the free dimensions carry and the share left over; and
\code{concept_variance}, one row per concept per modality.
}
\description{
Each modality is projected onto the vocabulary from \code{\link[=chorale_concepts]{chorale_concepts()}},
giving one score per concept per sample: the average standardised level of
the features that carry that concept. The score is a number about a named
piece of biology, and it means the same thing in every modality, which is
what lets modalities measured on different individuals be compared at all.
}
\details{
What the vocabulary does not explain is not discarded. The concept scores are
regressed out of the assay and independent component analysis is run on what
remains, so coordinated variation that no concept accounts for stays visible
as free dimensions rather than being absorbed into a concept that does not
describe it. The free dimensions are orthogonal to the concept scores by
construction, which is what stops the two channels reporting the same signal
twice.

Encoding never sees the phenotype. Nothing in this function reads the design,
so the scores cannot have been shaped by the contrast they are later asked
about.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 60, seed = 1)
ids <- sprintf("feature_\%05d", seq_len(60))
sim$modalities <- lapply(sim$modalities, function(m) {
  rownames(m) <- ids
  m
})
containers <- Map(chorale_load, sim$modalities, sim$col_data)
sets <- list(one = ids[1:20], two = ids[21:40])
cc <- chorale_concepts(containers, sets, min_features = 5)
chorale_encode(containers, cc, n_free = 2, n_init = 2)
}
