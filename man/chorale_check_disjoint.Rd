% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{chorale_check_disjoint}
\alias{chorale_check_disjoint}
\title{Whether any individual appears in more than one modality}
\usage{
chorale_check_disjoint(containers)
}
\arguments{
\item{containers}{A named list of \code{\link[=chorale_load]{chorale_load()}} containers, of design
tables carrying \code{sample_id}, or of feature-by-sample matrices whose column
names are the identifiers.}
}
\value{
A data frame with one row per identifier carried by more than one
modality, giving the identifier, the number of modalities carrying it and
their names. Empty where the collection is disjoint.
}
\description{
The whole method rests on the modalities being measured on disjoint sets of
individuals: no sample is shared, so the comparison of phenotype effects is
anchored on design information rather than on matched individuals. Each
container is validated on its own by \code{\link[=chorale_load]{chorale_load()}}, which cannot see that
assumption because it holds across containers rather than within one.
}
\details{
This collects every modality's \code{sample_id} values and reports each identifier
carried by two or more of them. A collision is not necessarily a shared
individual: independent studies reuse plain identifiers, so \code{S01} in one
deposit and \code{S01} in another may be two animals with one name. It is
therefore reported and not refused, and the analyst decides which of the two
it is.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 30, seed = 1)
containers <- Map(chorale_load, sim$modalities, sim$col_data)
chorale_check_disjoint(containers)
}
