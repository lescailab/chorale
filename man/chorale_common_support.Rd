% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohorts.R
\name{chorale_common_support}
\alias{chorale_common_support}
\title{The population a claim from this fit can be made about}
\usage{
chorale_common_support(fit, min_samples = 2L)
}
\arguments{
\item{fit}{A \code{chorale_fit} object.}

\item{min_samples}{Cells held by fewer than this many samples in a cohort
count as unpopulated there.}
}
\value{
A list with \code{cells}, the design cells common to every modality;
\code{share}, the fraction of each modality's samples those cells hold; and
\code{restricted}, true where any modality has samples outside the common
support.
}
\description{
The common support: the design cells every contributing cohort populates.
A programme recovered here describes those animals. Reporting it beside a
result is what keeps a claim inside the population the data cover, rather
than extending it to cells only one cohort holds.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 80,
                        n_shared_factors = 2, n_private_factors = 1,
                        n_strains = 4, n_per_cell = 3, seed = 1)
containers <- Map(chorale_load, sim$modalities, sim$col_data)
fit <- chorale_fit(containers, n_factors = c(3, 3), n_init = 2,
                   n_ambiguity_boot = 19)
chorale_common_support(fit)$cells
}
