% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concept_plant.R
\name{chorale_validate_concepts}
\alias{chorale_validate_concepts}
\title{Recovery of planted concepts across a grid of regimes}
\usage{
chorale_validate_concepts(
  grid,
  n_rep = 3L,
  n_permutations = 999L,
  n_free = 0L,
  alpha = 0.05,
  criterion = c("fdr", "family_wise"),
  seed = 1L
)
}
\arguments{
\item{grid}{A data frame of regimes. Any of \code{n_samples}, \code{n_features},
\code{effect}, \code{coverage}, \code{n_modalities}, \code{n_concepts} and \code{n_planted} may be
columns; those absent take their default. A \code{label} column is carried
through.}

\item{n_rep}{Collections drawn per regime.}

\item{n_permutations}{Permutations calibrating each fit.}

\item{n_free}{Free dimensions per modality.}

\item{alpha}{Threshold a concept is called at.}

\item{criterion}{Which control decides a call, as in
\code{\link[=chorale_score_concepts]{chorale_score_concepts()}}.}

\item{seed}{Integer seed; replicate \code{r} of regime \code{i} uses
\code{seed + 100 * i + r}.}
}
\value{
\code{grid} with the mean recovery over the replicates joined on, the
number of replicates that could be evaluated, and the reason where any
could not.
}
\description{
A single simulation says whether the estimator worked once. A curve says
where it stops working. Each row of the grid is a regime, several collections
are drawn from it, and the recovery of the planted concepts is averaged over
them.
}
\details{
Three dials matter and each asks a different question. \strong{Sample size} asks
whether bulk cohorts are large enough. \strong{Effect size} asks how strong a
concept's phenotype contrast has to be. \strong{Vocabulary
coverage} asks how far the vocabulary has to reach into a modality's
measured features, which is the question a modality with an incomplete
annotation raises.
}
\examples{
grid <- data.frame(label = c("strong", "absent"), effect = c(1, 0))
chorale_validate_concepts(grid, n_rep = 1, n_permutations = 99)
}
