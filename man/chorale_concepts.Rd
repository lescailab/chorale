% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concepts.R
\name{chorale_concepts}
\alias{chorale_concepts}
\title{The named concepts a collection can be scored on}
\usage{
chorale_concepts(
  containers,
  sets,
  feature_space = NULL,
  feature_map = NULL,
  min_features = 5L,
  min_modalities = 1L,
  min_lipid_compounds = 2L,
  min_lipid_specificity = 0.05,
  assay_name = NULL
)
}
\arguments{
\item{containers}{A named list of \code{\link[=chorale_load]{chorale_load()}} containers, one per
modality.}

\item{sets}{A named list of concepts, each a character vector of member
identifiers, as returned by \code{\link[=chorale_genesets]{chorale_genesets()}}.}

\item{feature_space}{Optional named character vector saying how each
modality's features reach the vocabulary: \code{"gene"} through gene
identifiers, \code{"lipid"} through lipid class. Modalities not named default to
\code{"gene"}.}

\item{feature_map}{Optional named list, one entry per modality, each a
mapping from \code{\link[=chorale_map]{chorale_map()}} carrying \code{id}, \code{ENTREZID} and \code{weight}. Where
a modality supplies one, a feature identifier matching several genes
contributes proportionally rather than counting once per gene.}

\item{min_features}{Features of a modality a concept must reach before that
modality is counted as expressing it. A concept matching one or two
measured features is matched by chance rather than measured.}

\item{min_modalities}{Modalities a concept must reach to enter the
vocabulary. The default keeps a concept reaching one modality, since it
still carries evidence there; raising it to two keeps only concepts that
can connect modalities.}

\item{min_lipid_compounds, min_lipid_specificity}{Passed to
\code{\link[=chorale_metabolite_matrix]{chorale_metabolite_matrix()}} for modalities whose feature space is
\code{"lipid"}.}

\item{assay_name}{Assay to read from each container. Defaults to the first.}
}
\value{
An object of class \code{chorale_concepts} with \code{vocabulary}, the
concepts retained; \code{membership}, one feature-by-concept matrix per
modality; \code{coverage}, one row per concept per modality carrying the number
of features that modality has for it; and \code{summary}, one row per concept
carrying how many modalities express it and whether all do.
}
\description{
Modalities measured on different individuals share no samples, so something
other than a sample must connect them. That something is a fixed vocabulary
of named biological concepts: a concept is a set of features, defined once
for the collection, and every modality that carries enough of those features
can be scored on it. A concept is the same concept in every modality, so no
correspondence between modalities has to be estimated.
}
\details{
Features reach a concept by the route their identifiers allow. Genes and
proteins reach it through gene identifiers, by way of
\code{\link[=chorale_geneset_matrix]{chorale_geneset_matrix()}}. Lipid species reach it through their class, by
way of \code{\link[=chorale_metabolite_matrix]{chorale_metabolite_matrix()}}, since a pathway acts on a class rather
than on one chain length. Either way the concept keeps its name, so the two
modalities end up in one vocabulary rather than in two that have to be
reconciled afterwards.

Coverage is reported rather than assumed. A concept expressible in every
modality of the collection can carry evidence across all of them; one
expressible in a subset carries evidence across that subset; one no modality
expresses carries none, and is reported as absent rather than silently
dropped, because an absence is a statement about the measured features.
}
\examples{
sim <- chorale_simulate(n_modalities = 2, n_features = 60, seed = 1)
containers <- Map(chorale_load, sim$modalities, sim$col_data)
ids <- rownames(sim$modalities[[1]])
sets <- list(concept_a = ids[1:20], concept_b = ids[15:40])
chorale_concepts(containers, sets, min_features = 5)
}
