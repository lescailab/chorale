% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{chorale_python_setup}
\alias{chorale_python_setup}
\title{Set up the Python environment for chorale}
\usage{
chorale_python_setup(envname = "r-chorale")
}
\arguments{
\item{envname}{Name of the managed conda environment to create or reuse.}
}
\value{
Invisibly, the path to the conda environment's Python
interpreter.
}
\description{
Where chorale is loaded inside an already-active conda environment, that
environment's interpreter is used directly (see \code{R/zzz.R} \code{.onLoad}) and
this function is not needed. Otherwise, it creates a managed conda
environment from the package's \code{inst/python/environment.yml} the first
time it is called, and binds reticulate to it. Delegating environment
creation to basilisk is deliberately excluded, since its isolated
environment would duplicate and shadow the interpreter a conda
installation already provides.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
chorale_python_setup()
\dontshow{\}) # examplesIf}
}
