% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{chorale_null_calibration}
\alias{chorale_null_calibration}
\title{Calibration of the joint null under no shared structure}
\usage{
chorale_null_calibration(
  n_sim = 100L,
  alpha = 0.05,
  n_perm = 200L,
  n_init = 5L,
  n_ambiguity_boot = 999L,
  ...,
  seed = 1L
)
}
\arguments{
\item{n_sim}{Number of null datasets.}

\item{alpha}{Threshold at which the false positive rate is reported.}

\item{n_perm}{Permutations calibrating each fit.}

\item{n_init}{Initialisations per fit.}

\item{n_ambiguity_boot}{Ambiguity-bootstrap replicates passed to each fitted
null dataset. The production default is 999; exploratory calibration may
use fewer for speed.}

\item{...}{Passed to \code{\link[=chorale_simulate]{chorale_simulate()}}, except \code{effect_size}, which is
fixed at zero.}

\item{seed}{Integer seed.}
}
\value{
A list with the collected \code{p_values}, the \code{ks_p} of their
uniformity test, and the \code{false_positive_rate} at \code{alpha}.
}
\description{
The p-value of a genuine null must be uniform, or the reported significance
does not mean what it says. Data with no design-linked shared structure are
simulated repeatedly, the estimator is run in full on each, and the joint
p-value of its strongest programme is collected. A Kolmogorov-Smirnov test of
those p-values against the uniform distribution is the calibration: a small
p-value there is evidence the procedure is miscalibrated, and the false
positive rate at a threshold reports how often a null run would be called a
discovery.
}
\examples{
chorale_null_calibration(n_sim = 10, n_perm = 99, n_init = 3,
                         n_ambiguity_boot = 19,
                         n_features = 120, n_per_cell = 3)
}
